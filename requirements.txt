# ATHENA - Requirements
# Autonomous Tactical Harvesting & Execution Network Architecture

# Core Python (3.10+)
# No external dependencies required for core ATHENA functionality

# The current implementation is SIMULATION MODE
# All components are self-contained Python with only standard library

# ═══════════════════════════════════════════════════════════════════════
# CURRENT STATUS: GitHub API integration uses stdlib only (urllib)
# ═══════════════════════════════════════════════════════════════════════

# Standard library only:
# - dataclasses (Python 3.7+)
# - typing (Python 3.5+)
# - datetime (built-in)
# - logging (built-in)
# - json (built-in)
# - pathlib (Python 3.4+)
# - enum (Python 3.4+)

# ═══════════════════════════════════════════════════════════════════════
# OPTIONAL: For PRODUCTION deployment (when you replace simulations)
# ═══════════════════════════════════════════════════════════════════════

# GitHub API integration now uses stdlib urllib (no external deps needed).
# These are optional for advanced usage:
# PyGithub>=2.1.1  # richer API wrapper if needed later

# Code parsing (for actual code extraction)
# tree-sitter>=0.20.4
# astroid>=3.0.0

# Testing frameworks (for real test execution)
# pytest>=7.4.0
# pytest-cov>=4.1.0
# pytest-benchmark>=4.0.0

# Security scanning
# bandit>=1.7.5
# safety>=2.3.5

# Performance testing
# locust>=2.15.0

# ═══════════════════════════════════════════════════════════════════════
# NOTES
# ═══════════════════════════════════════════════════════════════════════

# 1. GITHUB SCOUT: Uses real GitHub API via stdlib urllib
#    - Set GITHUB_TOKEN env var for authenticated access (5000 req/hr)
#    - Works unauthenticated with lower rate limits (60 req/hr)
#    - Falls back to simulation data if API unavailable
#    - Results cached with TTL (1hr search, 24hr file trees)

# 2. STILL SIMULATED:
#    - Code integration engine (code_integrator.py)
#    - Test execution (artemis.py)
#    - Full deployment pipeline

# 4. INSTALLATION (when ready):
#    pip install -r requirements.txt
